# JVM memory
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX
android.useAndroidX=true

# Stable Gradle behavior
org.gradle.vfs.watch=false

# Fix Kotlin daemon crash
kotlin.compiler.execution.strategy=in-process
